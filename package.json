{
  "name": "angelo_car",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --prefix apps/web",
    "vercel-build": "npm run build --prefix apps/web",
    "start": "npm run start --prefix apps/web",
    "dev": "npm run dev --prefix apps/web"
  }
}